/*  This is a long block comment.
    These are sometimes used to describe
    the purpose of a particular code file.
*/
// turn on if turned off
if (off) /* turn on! */ turnOn()
// check ourselves...
if (stressed) {
    breatheIn()	/* breathe in */
    breatheOut() /* breathe out */
} else if/* we are probably tired */(tired) {
    rest()	/* we're tired, so lets rest */
} else {
    allGood() /* Seems we're all good... /
}